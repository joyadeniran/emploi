import { BarChart3 } from "lucide-react";
import { PagePlaceholder } from "@/components/PagePlaceholder";

export const metadata = { title: "Career Insights — Emploi" };

export default function InsightsPage() {
  return (
    <PagePlaceholder
      icon={BarChart3}
      title="Career Insights"
      blurb="Salary benchmarks, skill demand and market trends for your role and region."
    />
  );
}
