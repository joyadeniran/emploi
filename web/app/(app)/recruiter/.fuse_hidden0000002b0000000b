import { Users } from "lucide-react";
import { PagePlaceholder } from "@/components/PagePlaceholder";

export const metadata = { title: "Recruiter Workspace — Emploi" };

export default function RecruiterPage() {
  return (
    <PagePlaceholder
      icon={Users}
      title="Recruiter Workspace"
      blurb="Shortlist verified, interview-ready candidates. Currently in private beta."
      note="We're onboarding recruiter teams gradually — contact hello@emploihq.com for early access."
    />
  );
}
