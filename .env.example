# Emploi environment — copy to .env or set in your host's dashboard.
# NEVER commit real values. .streamlit/secrets.toml is the Streamlit-native
# alternative (also gitignored).

# Required in production. Server-side only; users never see or enter a key.
GEMINI_API_KEY=

# Future (not read yet — reserved for the persistence rollout after auth):
# EMPLOI_DB_PATH=emploi.sqlite3
